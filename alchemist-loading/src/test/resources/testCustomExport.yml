incarnation: protelis

launcher:
  type: HeadlessSimulationLauncher

export:
  - type: CSVExporter
    parameters: ["test-custom-export"]
    data:
      - type: MeanSquaredError
        parameters: [ "A", "A", product, "A", "A" ]
