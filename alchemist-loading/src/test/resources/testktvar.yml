variables:
  a:
    formula: 22 + 1
    language: kts
  test:
    formula: |
      import it.unibo.alchemist.boundary.Loader
      import kotlin.collections.listOf
      val b = 5.5
      listOf(bindings["a"], b)
    language: kotlin
  test2:
    formula: |
      val b = 5.5
      listOf("a", b)
    language: kotlin
  test3:
    formula: test + test2
incarnation: sapere
