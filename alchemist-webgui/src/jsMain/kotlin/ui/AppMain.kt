/*
 * Copyright (C) 2010-2024, Danilo Pianini and contributors
 * listed, for each module, in the respective subproject's build.gradle.kts file.
 *
 * This file is part of Alchemist, and is distributed under the terms of the
 * GNU General Public License, with a linking exception,
 * as described in the file LICENSE in the Alchemist distribution's top directory.
 */

package ui

/*import components.Content
import io.kvision.Application
import io.kvision.panel.root
import org.w3c.dom.CanvasRenderingContext2D
import org.w3c.dom.HTMLCanvasElement
import web.dom.document
import kotlin.math.PI*/

/*val connection = ClientConnection();
private val scope = MainScope()
private const val UPDATE_STATE_DELAY: Long = 100

val App = FC<Props> {
    val (data, setData) = useState<NodeQuery.Data?>(null)

    useEffectOnce {
        scope.launch {
            val result = connection.retrieveQuery().await()
            setData(result)
        }
    }
    div {
        h1 {
            +"The result of the query:"
        }

        h2 {
            +NodeQuery.OPERATION_DOCUMENT
        }
        // Render data from GraphQL query
        div {
            +"Position is:${data?.nodePosition?.coordinates}"
        }
    }
}*/

/*class App : Application() {
    override fun start() {
        root("root") {
            add(Content())
        }
    }
}*/
